import React from 'react';
import {Header} from'../components';
const Lessons = () => {
    return(
        <div className="content">
            <Header onClick={()=>{}}/>
        </div>
    );
};

export default Lessons;